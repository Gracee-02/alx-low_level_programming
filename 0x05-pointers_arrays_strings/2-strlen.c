#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 *
 * @s: string lenght to be read
 *
 * Return: Lenght of the string
 *
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
